Bot written for Windows.

In order to use you need to change file run_bot.bat. Point your bot ID and check path,

Files ./handlers/admin.py and ./handlers/client.py should contain UID of telegram accounts in ID array. 

You should have a file of database sqlLite in root directory with name tel_bot_mem.db . You can change name in file ./data_base/sqlite_db.py
